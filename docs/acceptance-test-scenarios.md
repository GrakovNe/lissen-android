# Приемочные тесты: пользовательские сценарии

Статус: **реализовано** — `app/src/androidTest/kotlin/org/grakovne/lissen/ui/acceptance/`.
Отклонения от первоначального плана зафиксированы в разделе 8.

## 1. Цель

Проверить приложение как чёрный ящик цепочками «пользовательское действие → наблюдаемый
результат»: «запустил приложение → выбрал книгу → поставил на паузу → убедился, что стоит».
Дополняет существующие E2E-тесты (`app/src/androidTest/.../ui/`), которые сейчас покрывают
в основном «экран появился / навигация работает», но не состояние воспроизведения.

## 2. Принципы

- **Настоящий стек**: Compose UI-тесты (`createAndroidComposeRule<AppActivity>`) против живого
  сервера `demo.lissenapp.org` (параметры `e2eHost/e2eUsername/e2ePassword`, см. `E2ETestConfig.kt`).
- **Двойная проверка результата**: UI-локатор (testTag / contentDescription) + программное
  состояние (`MediaRepository.isPlaying`, `PlaybackService`, настройки, ответ сервера).
  «Поставил на паузу» = кнопка показывает «Play» И `isPlaying == false`.
- **Локаторы**: только существующие `testTag` и a11y-описания (`Play`, `Pause`,
  `Rewind %d seconds`, `Fast forward %d seconds`, `Next track`, `Previous track`).
  Новые testTag добавляем лишь при явной необходимости и фиксируем здесь.
- **Стабильность**: сессия логина переиспользуется через `E2ESession` (snapshot/restore);
  после каждого теста — остановка `PlaybackService` (как в `PlayerE2ETest`); таймаут 45 с;
  ни один тест не зависит от порядка, кроме явно помеченных.
- **Данные демо-сервера не трогаем**: тесты читают и воспроизводят, но не меняют библиотеку
  (кроме прогресса, это допустимо) и не удаляют книги.

## 3. Что уже покрыто (не дублируем)

| Область | Существующие тесты |
|---|---|
| Логин: экран, валидные/невалидные данные | `LoginFlowE2ETest` |
| Библиотека открылась, заголовок, тап по книге → плеер | `LibraryE2ETest` |
| Поиск в библиотеке | `LibrarySearchE2ETest`, `LinkedSearchE2ETest` |
| Плеер: номер главы, кнопка «назад», сохранение скролла | `PlayerE2ETest` |
| Настройки: sheet, logout, equalizer, advanced, backup | `SettingsE2ETest`, `ConfigBackupE2ETest` |
| Поворот экрана (landscape/portrait) | `LandscapeE2ETest`, `LandscapeLoginE2ETest` |

## 4. Новые сценарии

Приоритет: **P0** — ядро пользовательского пути, **P1** — важное, **P2** — желательное.

### 4.1 Воспроизведение (главный пробел)

| ID | Приоритет | Сценарий | Шаги | Ожидаемый результат |
|---|---|---|---|---|
| PB-1 | P0 | Пауза/продолжение (эталон пользователя) | Логин → открыть книгу → дождаться `playerScreen` и начала воспроизведения → тап Pause → тап Play | После паузы: кнопка с описанием «Play» отображена И `MediaRepository.isPlaying.value == false`. После Play: «Pause» И `isPlaying == true` |
| PB-2 | P0 | Прогресс идёт во время воспроизведения | Открыть книгу → считать позицию (contentDescription слайдера) → подождать ~3–5 с реального времени | Позиция увеличилась |
| PB-3 | P0 | Перемотка вперёд/назад | Открыть книгу → «Fast forward N seconds» → «Rewind N seconds» | Позиция выросла примерно на N, затем вернулась (допуск ±нескольких секунд) |
| PB-4 | P1 | Следующая/предыдущая глава | Открыть многотрековую книгу → «Next track» → «Previous track» | Номер главы (`playerChapterNumber`) изменился и вернулся |
| PB-5 | P1 | Выбор главы из списка | Открыть книгу → `chapterList` → тап по другой главе | Воспроизводится выбранная глава (`playerChapterNumber` совпадает) |
| PB-6 | P1 | Скорость воспроизведения меняется и сохраняется | Открыть книгу → sheet скорости (`PlaybackSpeedComposable`) → выбрать 1.5x → перезапустить приложение → открыть книгу | Скорость 1.5x активна после рестарта (UI + `PlaybackPreferences`) |
| PB-7 | P1 | Таймер сна | Открыть книгу → `TimerComposable` → установить короткий таймер | Таймер активен в UI; по истечении — пауза (тест с коротким значением, P2-вариант — без ожидания) |
| PB-8 | P1 | Закладка | Открыть книгу → sheet закладок (`BookmarksComposable`) → добавить | Новая закладка появилась в списке; после sync есть и на сервере (проверка через API — P2) |
| PB-9 | P0 | Фоновое воспроизведение | Открыть книгу → играет → свернуть приложение (HOME) | `isPlaying == true`; уведомление воспроизведения существует (`NotificationManagerCompat` для канала плеера) |
| PB-10 | P1 | Mini-player | Играет книга → выйти в библиотеку → `miniPlayer` виден → тап | Открылся `playerScreen` той же книги |
| PB-11 | P1 | Прогресс синхронизируется с сервером | Открыть книгу → поиграть ~10 с → пауза → дождаться sync → REST-запрос к серверу (progress) | Серверная позиция ≈ позиция в приложении |

### 4.2 Сессия и запуск

| ID | Приоритет | Сценарий | Шаги | Ожидаемый результат |
|---|---|---|---|---|
| SE-1 | P0 | Сессия переживает рестарт | Логин → полностью перезапустить приложение (finish + relaunch) | Без экрана логина сразу открылась `libraryScreen` (токен сохранён) |
| SE-2 | P1 | Прерванное воспроизведение продолжается с позиции | Играть → пауза → выйти → снова открыть ту же книгу | Плеер открыт, позиция ≈ прежняя (±несколько секунд) |
| SE-3 | P2 | Logout завершает воспроизведение | Играть → Settings → logout | `isPlaying == false`, сервис остановлен, показан `loginScreen` |

### 4.3 Библиотека и контент

| ID | Приоритет | Сценарий | Шаги | Ожидаемый результат |
|---|---|---|---|---|
| LB-1 | P1 | Открытие серии | Библиотека с сериями → `seriesItem_*` → список книг серии → тап книги | `playerScreen` открылся |
| LB-2 | P2 | Смена библиотеки | `libraryNavBarTitle` → выбрать другую библиотеку | `libraryScreen` с новым заголовком, книги загрузились |
| LB-3 | P1 | Подкаст: эпизоды | Открыть подкаст → список эпизодов → воспроизвести эпизод | Играет (`isPlaying == true`), эпизод отмечен играющим |
| LB-4 | P2 | Поиск без результатов | Поиск заведомо отсутствующего запроса | Пустое состояние, приложение не упало |

### 4.4 Кэш и офлайн

| ID | Приоритет | Сценарий | Шаги | Ожидаемый результат |
|---|---|---|---|---|
| OF-1 | P1 | Скачивание книги | Лист книги → скачать (download-кнопка) → дождаться статуса «скачано» | Индикатор кэша в UI; запись в `LocalCacheRepository` |
| OF-2 | P1 | Офлайн-воспроизведение | После OF-1 → включить airplane mode → открыть скачанную книгу | Воспроизведение идёт без сети (`isPlaying == true`) |
| OF-3 | P2 | Удаление из кэша | Cached items settings → удалить книгу | Статус «не скачано»; офлайн-запуск книги даёт понятную ошибку, не краш |

### 4.5 Настройки, влияющие на пользователя

| ID | Приоритет | Сценарий | Шаги | Ожидаемый результат |
|---|---|---|---|---|
| ST-1 | P1 | Смена темы | Settings → тема Dark → назад | UI перекрашен (проверка через `AppearancePreferences` + контраст фонового цвета); сохранена после рестарта — **не реализован, удалён** (п. 8.14) |
| ST-2 | P2 | Эквалайзер влияет на звук | Настроить полосу → воспроизвести | Полоса сохранена (персист — уже покрыт; здесь — не ломает воспроизведение) |

## 5. Ограничения и риски (важно для решения)

1. **Сеть**: 32 падения прошлых прогонов — таймауты demo-сервера из эмулятора (RTT ~360 мс).
   Для новых P0-тестов предлагаю: увеличенный таймаут ожидания буферизации и повторный
   `performClick` при отсутствии реакции; тесты с `PB-11`, `OF-1/2` заведомо медленные —
   вынести в отдельный тег/задачу (`@LargeTest`), гонять отдельно от PR-цикла.
2. **Реальные аудио-потоки**: проверка «позиция выросла» требует, чтобы сервер реально отдавал
   аудио; на демо-сервере это так, но тесты чувствительны к загрузке канала — допуски по
   позиции generous (±5 с), проверки «выросло», а не «выросло ровно на N».
3. **Таймер сна с ожиданием срабатывания** (PB-7 полный) — минимум 1 минута реального времени;
   предлагаем в PR-цикл включать только укороченную версию.
4. **Airplane mode (OF-2)** требует `WRITE_SETTINGS`/shell-доступа на эмуляторе
   (`adb shell svc wifi data offline`) — реализуемо в нашем CI-эмуляторе, но это уже не чистый
   UI-тест; альтернатива — имитация через недоступность хоста (iptables/плохой DNS) — тоже shell.
5. **Локализаторы позиции**: contentDescription слайдера — это текст позиции; если формат
   изменится, тесты упадут — это осознанный «хрупкий» слой, держим парсинг в одном helper.

## 6. Предлагаемая структура реализации (после утверждения)

```
app/src/androidTest/kotlin/org/grakovne/lissen/ui/acceptance/
  PlaybackAcceptanceTest.kt      // PB-1..PB-5, PB-9, PB-10
  SessionAcceptanceTest.kt       // SE-1..SE-3
  ContentAcceptanceTest.kt       // LB-1..LB-4
  OfflineAcceptanceTest.kt       // OF-1..OF-3  (@LargeTest)
  SettingsImpactAcceptanceTest.kt// ST-1, ST-2
  AcceptanceFixtures.kt          // общий helper: открыть первую книгу,
                                 // читать позицию, ждать isPlaying, shell-хелперы
```

Общий helper переиспользует `E2ETestConfig.kt` (login, session snapshot, `bookItemMatcher`).

## 7. Реализация

Классы: `PlaybackAcceptanceTest` (PB-1..7, PB-9..11), `SessionAcceptanceTest` (SE-1..3),
`ContentAcceptanceTest` (LB-1..4), `OfflineAcceptanceTest` (OF-1..2),
`SettingsImpactAcceptanceTest` (ST-2), общие хелперы — `AcceptanceFixtures.kt`.

## 8. Отклонения и допущения реализации

**Принцип: прод-код не изменяется.** Тесты пишутся против существующего поведения;
если сценарий без правки прода непроходим или нестабилен — сценарий удаляется.

1. **Без `@LargeTest`** (решение пользователя): сьют может быть медленным.
2. **Airplane mode** — через `UiAutomation.executeShellCommand("cmd connectivity
   airplane-mode enable|disable")`, без root и без WRITE_SETTINGS.
3. **Тестовых тегов в проде нет.** Иконки без a11y-описаний кликаются по геометрии
   (позиция относительно соседнего узла с тегом/текстом) или по тексту строки.
4. **Изоляция тестов — только тестовыми средствами** (`releasePlayback` в
   `AcceptanceFixtures`): `clearPlayingBook()`, release контроллера через reflection,
   `stopService(PlaybackService)`, `SimpleCache.release()`. Это компенсирует то, что
   Hilt пересоздаёт singleton-граф на каждый тест, а процесс и сервис — нет.
5. **Таймер сна (PB-7)** — короткая версия: таймер выставлен и активен; ожидание
   срабатывания (минимум 10 минут — минимальный пресет) не проверяется.
6. **LB-1/LB-2/LB-3** используют `Assume.assumeTrue`: на демо-сервере одна книжная
   библиотека без серий и подкастов. Для полного покрытия демо-серверу нужны серия,
   вторая библиотека и подкаст.
7. **Серверные проверки (PB-11, выбор книги)** — `E2EServerClient` на OkHttp поверх
   API Audiobookshelf (`/login`, `/api/libraries`, `/api/me/progress`); тесты открывают
   самую маленькую книгу библиотеки. `GET /api/me/progress/{id}` отдаёт объект
   прогресса без обёртки `data`.
8. **PB-4** учитывает семантику `previousTrack`: первый тап перематывает текущую главу
   в начало (порог 5 с), только затем переход к предыдущей; направление выбирается по
   текущей позиции.
9. **PB-6/PB-7** кликают иконки Speed/Timer через `useUnmergedTree = true`.
10. **PB-9**: на Android 14+ уведомление foreground-сервиса не имеет
    `FLAG_ONGOING_EVENT`, проверка идёт по `FLAG_FOREGROUND_SERVICE`.
11. **Удалённые сценарии** (решение пользователя «оставить проходящее, удалить
    непроходящее»):
    - **PB-8 (создание закладки)** — клики по иконкам без a11y-меток нестабильны;
    - **ST-1 (смена темы)** — после `recreate()` библиотека не прогруживалась из-за
      нестабильного DNS эмулятора; персистентность схемы покрыта unit-тестами
      `AppearancePreferences`;
    - **OF-3 (очистка кэша и офлайн-плей)** — «Clear downloaded chapters» в текущем
      проде вызывает `navController` не из main-потока (краш); без правки прода
      непроходимо.
12. **SE-3 переименован в `se03_logoutReturnsToLogin`**: фактическое поведение прода —
    logout показывает экран логина, но плеер освобождается только при возврате на
    `LibraryScreen`, поэтому синхронная остановка воспроизведения не проверяется.
13. **Возврат из настроек** (ST-2) — аппаратная «назад» требует `ACTION_DOWN` +
    `ACTION_UP`; только DOWN не обрабатывается predictive back.
14. **Скролл библиотеки** — `scrollToBookInLibrary` сначала прокручивает сетку к
    началу (`performScrollToIndex(0)`): сетка сохраняет позицию между навигациями,
    а `performScrollToNode` скроллит только вперёд.
