<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="32" />

    <application
        android:name=".LissenApplication"
        android:allowBackup="true"
        android:hasFragileUserData="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.Lissen"
        tools:ignore="DiscouragedApi,UnusedAttribute"
        android:manageSpaceActivity="org.grakovne.lissen.content.LissenDataManagementActivity"
        tools:targetApi="36">

        <!-- Add Android Auto support -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.activity.AppActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Lissen"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Lets the user open an exported settings backup directly (e.g. from a file
                 manager) instead of hunting for the import picker inside the app. Matched by
                 filename rather than a generic MIME type so the app doesn't show up for every
                 JSON file - only for its own exports. android:pathPattern's "*" only repeats the
                 single preceding character (no regex-style "any characters" wildcard), so a
                 variable-prefix path can't be expressed with it - pathSuffix (API 31+) is used
                 for the external storage provider instead, and our own FileProvider path is
                 fully known ahead of time so it's matched as a literal. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:scheme="content"
                    android:host="com.android.externalstorage.documents"
                    android:pathSuffix="lissen-backup.lsettings" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:scheme="content"
                    android:host="${applicationId}.fileprovider"
                    android:path="/logs/lissen-backup.lsettings" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.grakovne.lissen.content.LissenDataManagementActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:screenOrientation="portrait"
            android:name=".channel.audiobookshelf.common.oauth.AudiobookshelfOAuthCallbackActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="lissen"
                    android:host="oauth" />
            </intent-filter>
        </activity>

        <service
            android:name=".playback.service.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
            </intent-filter>
        </service>

        <service
            android:name=".content.cache.persistent.ContentCachingService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:enabled="true" />

        <receiver
            android:name=".widget.state.PlayerStateWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_state_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.cover.PlayerCoverWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_cover_widget_info" />
        </receiver>

        <provider
            android:name=".content.ExternalCoverProvider"
            android:authorities="${applicationId}.cover"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/log_file_paths" />
        </provider>

    </application>

</manifest>
